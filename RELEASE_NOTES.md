# KTR Shot 0.3.1 - KTR2 Only

**仅用于 KTR2 掌机，实测系统为 Android 14。请勿在其他型号或普通 Android 手机上安装使用。** 本应用为独立工具，非设备厂商官方应用；未实现机型安装锁定。

## 下载与安装

- `KTR-Shot-0.3.1-KTR2-release.apk`：使用项目专用证书签名的 Release 构建，不启用 Android 调试模式。
- `SHA256SUMS`：APK 的 SHA-256 校验值。
- 安装后打开应用，在系统无障碍设置中开启 **KTR Shot 截图服务**，保留 KOS 原有服务。
- 不需要 root、Shizuku 或电脑激活。日常暂停使用应用内开关，不要强行停止应用。

**从此前 debug 测试版迁移：** 本次正式签名与测试版不同，不能直接覆盖安装。请先记录自定义快捷键和延迟，再卸载测试版、安装本版并重新启用无障碍服务。卸载会清除应用设置；重要截图请先自行备份。本次公开源码整理后完成了构建检查，当前功能此前已在 KTR2 真机验证；正式签名 APK 未重新进行真机安装测试。

## 当前功能

- 默认全局 **MENU + SELECT** 截图，可学习单键或组合键。
- PNG 保存到 **Pictures/KTRShots**；成功保存后播放一次系统“咔嚓”声。
- 无截图弹窗、确认框、闪屏或预览；失败时仅在应用设置页显示状态。
- 截图延迟 0–500 ms，首次安装默认 80 ms，可设为 0 ms 取消主动等待。0 ms 不代表系统截图和保存没有耗时。
- 长按不连拍，两键全部松开后才能再次触发；最短截图间隔约 800 ms。
- 不吞键、不补发、不创建虚拟手柄、不独占输入设备。KOS 或模拟器原有快捷键动作仍可能同时发生。

## 限制与隐私

- 仅支持 KTR2，其他固件或模拟器组合未全部验证；无障碍调度和截图处理可能影响游戏帧时间。
- 受保护窗口或 DRM 内容可能无法截图或得到黑图，不绕过系统保护。
- 无联网权限、不读取窗口文字、不上传图片。
- 快门音的音量和静音行为由系统固件决定，不强行调整音量。

源码按 **GPL-3.0-only** 发布，详见仓库中的 LICENSE 和第三方许可说明。